import UIKit
/*: ## Assignment 1: Git & GitHub
 ### Part 2
* git ststus:查看目前git狀態
* git add:將files加入到Staging area
* git commit:將Staging area的資料放到local Repo
* git log:查看所有commit
* git push:將local Repo上傳到Github
* git remote -v:查看已連結的遠端數據庫
* git branch:顯示分支列表，有顯示*代表現在的分支
* fork:在Github上複製一份別人的文件到自己的Github中
 
 ### Part 3
 登入Github在右上帳號旁邊點選"+"符號後點選Create repository，輸入Repository name後往下拉點選Public設為公開，再依需求選擇是否要加入README file跟Choose a license，創建完成後複製Repo的HTTPS網址，回到git輸入git cd進入到要上傳至Github的檔案中輸入"git remote add origin HTTPS網址"，再輸入”git remoto -vv “ 查看確認已連結的遠端數據庫，最後輸入"git push --set-upstream orinin master" 完成upload!

 *   輸入"git push --set-upstream orinin master"會出現需要輸入github帳號名稱及token， token需要進入到github中的Personal access tokens設定Generate new token會得到一組token，如果是使用Mac的也可以在電腦中的Keychain Access設定儲存帳號密碼作集中管理。
 
*/

//: [⇒ Next: Basic](@next)
